apiVersion: v1
kind: Service
metadata:
  name: video-processing-service
  namespace: fiapx
spec:
  selector:
    app: video-processing-api
  ports:
    - port: 8086
      targetPort: 8086
  type: ClusterIP
