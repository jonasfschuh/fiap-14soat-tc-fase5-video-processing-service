<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>br.com.fiap</groupId>
        <artifactId>video-processing-ms</artifactId>
        <version>1.0.0</version>
    </parent>

    <artifactId>video-processing-report-aggregate</artifactId>
    <packaging>pom</packaging>
    <name>Video Processing Report Aggregate</name>
    <description>Módulo de agregação de cobertura JaCoCo dos 3 módulos: domain, infrastructure e application</description>

    <dependencies>
        <dependency>
            <groupId>br.com.fiap</groupId>
            <artifactId>video-processing-domain</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>br.com.fiap</groupId>
            <artifactId>video-processing-infrastructure</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>br.com.fiap</groupId>
            <artifactId>video-processing-application</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>check-coverage</id>
                        <phase>none</phase>
                    </execution>
                    <execution>
                        <id>aggregate-report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report-aggregate</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/site/jacoco-aggregate</outputDirectory>
                            <title>Video Processing MS - Cobertura Agregada</title>
                            <excludes>
                                <exclude>**/VideoProcessingApplication.class</exclude>
                                <exclude>**/AwsConfiguration.class</exclude>
                                <exclude>**/SwaggerConfiguration.class</exclude>
                                <exclude>**/VideoStorageBeanConfiguration.class</exclude>
                                <exclude>**/SchedulingConfiguration.class</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
